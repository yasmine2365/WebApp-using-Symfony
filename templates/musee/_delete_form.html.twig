<form method="post" action="{{ path('app_musee_delete', {'idMusee': musee.idMusee}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ musee.idMusee) }}">
    <button class="btn">Supprimer</button>
</form>
