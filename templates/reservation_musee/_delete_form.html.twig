<form method="post" action="{{ path('app_reservation_musee_delete', {'reservationId': reservation_musee.reservationId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_musee.reservationId) }}">
    <button class="btn">Supprimer</button>
</form>
